# 三国志 · 天下争锋

原创三国志类回合制策略游戏（Cocos Creator 3.8 + 官方逻辑还原）。

## 官方流程（当前实现）

```
主菜单 → 选剧本 → 选势力 → 战略地图 → 选城 → 命令大类 → 结束回合
                              ├ 内政：开发/开垦/治理
                              ├ 军事：征兵/出兵/运输/联合军/一骑讨/伏兵
                              ├ 人才：移动/赏赐/任命/搜索/登用在野武将
                              ├ 计谋：火计/离间/扰乱/伪报/鼓舞
                              └ 外交：同盟/停战/宣战/赠礼
         → AI 回合（含运输/伪报/联合军）→ 月结算 → 下一回合
```

## 剧本

| 剧本 | 说明 |
|------|------|
| 三足鼎立 | 200年标准开局，24武将 |
| 赤壁争锋 | 208年前线对峙，在野名将（庞德、徐晃、张郃、太史慈） |

## 运行

```bash
npm test           # 单元测试
npm run flow       # CLI 流程演示
npm run simulate   # 多回合模拟
npm run generate:audio      # 生成 WAV 音效
npm run generate:portraits  # 生成武将立绘 PNG
```

Cocos Creator：打开 `Game` 场景 → 预览；主菜单右下角应显示 `UI-v1.7.0`。Logo 使用 `resources/brand/logo2.webp`。

大厅二级页（存档列表、选剧本、剧本详情、选势力、武将图鉴、背景图鉴、设置）统一为文字菜单 + 大厅背景风格。

### 打包（Android 竖屏 + 启动图标）

游戏 UI 为竖屏 720×1280。打包前执行：

```bash
npm run setup:build   # 生成 PNG 图标、写入竖屏配置、替换 native/res 启动图标
```

然后在 Cocos **构建发布 → Android** 中确认：
- **屏幕方向**：仅勾选 **Portrait**（竖屏）
- 重新 **构建** 并 **生成** APK

说明：`resources/icons/` 仅用于游戏内显示；桌面启动图标来自 `build-assets/icons/app-icon.png`（默认 icon_01），由 `setup:build` 写入 `native/engine/android/res/mipmap-*/ic_launcher.png`。

## 开发路线

| 阶段 | 内容 | 状态 |
|------|------|------|
| v0.3 | 联合军、一骑讨、攻城、搜索 | ✅ |
| v0.4 | 多剧本、在野武将、AI计谋 | ✅ |
| v0.5 | 官方式 UI 布局 | ✅ |
| v0.6 | 设置页、战斗动画、回合遮罩 | ✅ |
| v0.7 | WAV 音效、武将头像、战斗过场 | ✅ |
| v0.8 | 立绘 PNG、情报/日志、存档预览 | ✅ |
| v0.9 | 地图装饰、邻城高亮、外交面板 | ✅ |
| v1.0 | 屏幕适配、战略地图、武将编辑、粮耗/忠诚/计谋成功率 | ✅ |
| v1.1 | 多存档槽、战术战、伪报/鼓舞、叛逃、势力统计、日志滚动 | ✅ 当前 |
